# OpenLess PR #497 Windows Capsule HITL Evidence

Evidence for `4b28d92 fix(windows): restore capsule transparent host boundary`.

Human gate: PASS.

The crop screenshot shows the Windows capsule over a solid green background. Pixel samples in `meta.json` confirm the native host margins remain transparent and no host-sized gray rectangle is visible.
